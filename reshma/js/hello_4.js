function abc(a)
{
    let fs = a.substring (0,1).toUpperCase()
    let ls = a.substring (1, a.lenght)
    return fs+ls
}
let result = abc("hamidkhan")

console.log(result)

function efg(a)
{
    let fs = a.substring (0,1).toUpperCase()
    let ls = a.substring (1, a.lenght)
    return fs+ls
}
let result1 = abc("hamidkhan")

console.log(result1)

